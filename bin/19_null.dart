void main() {
  /**
   * kata kunci null merupakan tipe data representasi data kosong
   * secara default, saat membuat variable , nilainya harus di isi  jika tidak maka variable tersebut tidak bisa di gunakan
   * Namu jika kita ingein membuat variable bisa di isi dengan data null kita bisa tambahkan karakter ? (tanda tanya) seperti:
   * TypeData? namaVariable = null;
   */

  // kode null

  //error
  // int variable1 = null;
  //print(variable1);

  //sukses
  String? variable2 = null;
  print(variable2);
}
