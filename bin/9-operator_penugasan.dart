void main() {
  /**
   * Operator penugasan
   * penambahan
   * a= a+10; => a +=10;
   * pengurangan
   * a= a-10 => a-=10;
   * perkalian
   * a = a*10; => a *=10;
   * pembagian hasil desimmal
   * a = a / 10; => a /=10;
   * pembagian hasil integer
   * a = a ~/10 => a ~/=10;
   * Sisa bagi
   * a= a %10; => a %=10;
   */

  /**
   * Increment dan Decrement
   * ++var  artinya var = var + 1 (ekpresionnya adalah var + 1 );
   * var++ artinya  var = var +1 (ekpresion adalah is var)
   */

  // incerment artinya menambahkan 1 nilai(atau kenaikan satu nilai daro nilai variable tersebut)

  // contoh increment
  var i = 0;
  var x = i++;
  print(i);

  var k = 0;
  var y = ++k;
  print(k);
}
